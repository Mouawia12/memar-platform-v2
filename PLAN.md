# خطة مشروع منصة معمار — Memar Platform

> **المرجع:** عرض `Memar_Full_Offer_Platform_Apps_2026-07-12.pdf` + محادثة مستقل مع العميل أيمن + وصف المشروع الأصلي.
> **الصفقة:** المنصة — $900 / 50 يومًا (تنفيذ + تجربة) · تاريخ الصفقة 2026-07-14 · التسليم 2026-09-02.
> **قرار النطاق:** تنفيذ **كل** الميزات ضمن العقد الحالي.
> **قاعدة البيانات:** MySQL (XAMPP للتطوير · MariaDB على سيرفر Synology للإنتاج).
> **الباك اند:** **Laravel (PHP)** — أصلي مع MySQL/XAMPP.
> **الحالة:** قيد التنفيذ — الباك اند والواجهة مبنيان ويعملان.
>
> **آخر تدقيق للقائمة: 2026-09-11.** كل بند مؤشَّر أدناه تحقّقتُ منه في الكود لا من الذاكرة.
> 🟡 = مبنيّ جزئيًّا، والناقص مكتوب في نص البند. ما بقي `[ ]` بلا علامة فلم يُبدأ.
> مرجع الجودة وقتها: 426 اختبارًا خلفيًّا ناجحًا · Pint نظيف · بناء الواجهة ناجح.

---

## قرار المعمارية (Architecture)

**الطبقة الموحّدة:** الواجهة (HTML/JS الحالية) → **Laravel REST API** → **MySQL**. (المتصفح لا يتصل بقاعدة البيانات مباشرة — كل شيء عبر API + JSON).

- [ ] **بناء باك اند جديد بـ Laravel** (Eloquent ORM، Migrations، Controllers، Form Requests).
- [ ] المصادقة عبر **Laravel Sanctum** (توكن API — يصلح لتطبيق الموبايل لاحقًا) + bcrypt.
- [ ] تخزين الملفات عبر **Laravel Storage** على قرص السيرفر (NAS).
- [ ] التواصل الفوري (دردشة/إشعارات لحظية) عبر **Laravel Reverb + Echo** (WebSockets).
- [ ] توليد PDF عبر حزمة Laravel (dompdf / Browsershot).
- [ ] المهام الثقيلة (توليد المستندات، الإشعارات المجدولة) عبر **Queues + Scheduler**.
- [ ] **إعادة ربط الواجهة الحالية** (`erp/erp_app.js` + `shared/*`) لتستدعي Laravel API بدل Supabase المباشر و`localStorage` الوهمي.
- [ ] **الحفاظ على تصميم الواجهة الحالي** الذي أعجب العميل (نعيد التوصيل لا الشكل).
- [ ] **إلغاء باك اند Node/Express** (`src/`, `server.js`, Socket.io) بعد نقل المنطق المفيد منه (RBAC، منطق الوحدات).
- [ ] (اختياري — يُناقَش) استخدام **Filament** كلوحة إدارة داخلية سريعة لبعض وحدات الـERP.

---

# الجزء الأول — متطلبات العميل (Checklist)

## 1) نشر وتشغيل المنصة على سيرفر العميل
- [ ] تجهيز بيئة التشغيل على سيرفر Synology (Node.js + قاعدة MariaDB).
- [ ] رفع المشروع وتشغيله بشكل دائم (PM2 / systemd).
- [ ] ربط الدومين + تفعيل شهادة SSL (HTTPS).
- [ ] إعداد Reverse Proxy (Nginx) أمام تطبيق Node.
- [ ] نسخ احتياطي دوري لقاعدة البيانات والملفات.
- [ ] اختبار استقرار كامل بعد النشر (Smoke test).
- [ ] تسليم تدريجي: رفع كل تحديث على السيرفر ليتابعه العميل أولًا بأول.

## 2) الميزات الست المضمونة (يجب أن تعمل بالكامل)

### 2.1 التسعير التلقائي (Auto Pricing) — *موجود، يحتاج ربط بقاعدة البيانات*
- [ ] مراجعة محرك التسعير الحالي (4 خطوات: قطاع/نوع → مساحة → باقات وخدمات → بيانات → عرض).
- [ ] 🟡 نقل بيانات التسعير إلى MySQL — جدول `services` مبني؛ الباقات والرسوم الحكومية وأسعار القطاعات وشرائح المساحات لم تُنقل بعد.
- [ ] 🟡 لوحة إدارة الأسعار — شاشة الخدمات مبنية (`/services`)؛ الباقات والرسوم ناقصة.
- [ ] حساب السعر النهائي (رسوم + إضافات + خصومات + تعديل يدوي) بدقة.

### 2.2 توليد عروض الأسعار التلقائي (Auto Quotations) — *موجود، يحتاج حفظ حقيقي*
- [ ] 🟡 توليد العرض من محرك التسعير — العرض يُبنى ببنود يدوية؛ الربط بمحرّك التسعير ناقص.
- [x] ربط العرض بالعميل والمشروع وحفظه في قاعدة البيانات — `quotations` + `quotation_items`، مُختبَر.
- [x] ترقيم تلقائي `QT-NNNN` + تتبع الحالة — مُختبَر.
- [ ] تصدير PDF على الخادم — dompdf مثبّت لكنه غير مستعمل؛ الطباعة حاليًا من المتصفح.
- [x] تحويل العرض المقبول إلى عقد ثم جدول دفعات (فواتير) — مُختبَر، ولا يُحوَّل العرض مرتين.

### 2.3 أتمتة تجهيز المستندات (Document Automation) — *وهمي حاليًا، بناء من الصفر*
- [x] تعريف قوالب المستندات/العقود بحقول `{{متغيّرة}}` — `document_templates` + محرّر نصوص.
- [x] محرك تعبئة القوالب — `DocumentService::generate`، والقيم تُهرَّب قبل الحقن (مُختبَر).
- [ ] توليد PDF فعلي على السيرفر — لم يُبنَ (الطباعة حاليًا من نافذة المتصفح).
- [ ] تحميل مستند مفرد + تحميل حزمة ZIP لعدة مستندات.
- [x] حفظ المستندات المولّدة وربطها بالمشروع — تظهر للعميل في بوابته.

### 2.4 الشات بوت (Chatbot) — *موجود، يحتاج مفتاح API وضبط*
- [x] الردود الذكية عبر OpenAI والمفتاح في `.env` بالخادم — `ChatbotService`.
- [ ] 🟡 قاعدة المعرفة — ردود بالقواعد (`ruleBased`) مبنية كخط أول واحتياطي؛ قاعدة المعرفة القابلة للتحرير ناقصة.
- [ ] تنفيذ الإجراءات من المحادثة (فتح حاسبة التسعير / حجز اجتماع).
- [ ] لوحة إدارة الشات بوت (تعديل المعرفة، مراجعة الأسئلة بلا إجابة).
- [ ] الاختيار النهائي لمزوّد AI والتكلفة (يُناقَش مع العميل).

### 2.5 الحضور والانصراف / البصمة أونلاين — *ناقص، بناء من الصفر*
- [x] تسجيل حضور/انصراف أونلاين — `/attendance/check-in` و`check-out`.
- [x] تحديد الموقع (lat/lng) والوقت عند التسجيل — مع تعليم التأخير تلقائيًّا.
- [ ] ⏸️ **"البصمة أونلاين" — مؤجّلة (نرجع لها لاحقًا)**؛ نصمّم جدول الحضور مرنًا ليستوعبها عند العودة.
- [x] سجل الحضور لكل موظف + ملخّص للإدارة — `/attendance/mine` و`/attendance/summary`.
- [ ] ربط الحضور بخصومات الرواتب تلقائيًا — الخصم يُدخَل يدويًّا حاليًا.

### 2.6 اجتماعات الفيديو على المنصة — *بدائي، يحتاج دمج كامل*
- [ ] 🟡 اجتماع فيديو مدمج — غرفة Jitsi تُولَّد وتُربط بالموعد، لكن الدخول عبر رابط خارجي لا داخل المنصة.
- [ ] دخول الاجتماع من الـERP وبوابة العميل بنفس التجربة.
- [ ] توليد غرفة/توكن من الباك اند (Jitsi / بديل).
- [x] ربط الاجتماع بالموعد/المشروع + الموظف المكلَّف.

## 3) الصفحة الرئيسية والموقع العام (إعادة تصميم)
- [x] إعادة تصميم الصفحة الرئيسية (React عام على /) — هيرو كاروسيل + خدمات + معرض أعمال + تذييل، مُختبَر.
- [x] الهيرو: كاروسيل تلقائي (3 شرائح دعائية) — الإدارة من لوحة التحكم لاحقًا.
- [x] معرض الأعمال (Portfolio) — شبكة أعمال في الصفحة الرئيسية · الإدارة لاحقًا.
- [x] **منتدى (Forum)** — مبني (أقسام/مواضيع/ردود/مشاهدات) على React+API، مُختبَر · التكامل مع الموقع العام لاحقًا:
  - [x] أقسام/مواضيع + إنشاء موضوع + الردود — مُختبَر.
  - [ ] إعجاب/تصويت + وسم أفضل إجابة.
  - [x] صلاحيات المنتدى — `forum.view/manage/delete`، مُختبَرة (بما فيها الحذف).
- [ ] تحسين نسخة الجوال (Responsive) للموقع العام.

## 4) وحدات الـERP (مراجعة + إصلاح + تطوير)
- [x] لوحة التحكم الرئيسية — مؤشرات حقيقية من قاعدة البيانات.
- [x] CRM والعملاء والشركات بأسلوب Odoo — Pipeline بمراحل، أنشطة، متابعات، وسوم — مُختبَر.
- [x] إدارة المشاريع — مراحل وقوالب مراحل وأعضاء ومحادثات — مُختبَرة.
- [ ] 🟡 المهام والمتابعة — لوحة Kanban ونطاقات وتعليقات مبنية ومُختبَرة؛ تسجيل الوقت ناقص.
- [ ] 🟡 المواعيد — تقويم وحجز ومكلَّف وغرفة فيديو مبنية؛ RSVP ناقص.
- [ ] 🟡 التواصل الداخلي — المحادثات وحفظ الرسائل مبنية ومُختبَرة، والتحديث اللحظي عبر نبضة `/sync/pulse` بدل WebSocket.
- [x] إدارة الموظفين (HR) — موظفون ومستندات وإجازات وخدمة ذاتية — مُختبَرة.
- [ ] 🟡 الرواتب — الاحتساب والبدلات والخصومات مبنية؛ الربط التلقائي بالحضور ناقص.
- [x] العقود — إنشاء وإدارة وربط بالمشروع، ومسودة عقد تلقائية لكل مشروع — مُختبَرة.
- [ ] 🟡 الفواتير — الإصدار والترقيم والتحصيل وانتقال الحالة ومتابعة المستحقات مبنية ومُختبَرة؛ بوابة دفع KNET الفعلية ناقصة (KNET مسجَّل كطريقة دفع يدوية فقط).
- [x] التقارير المالية — دخل/مصروفات/صافي/هامش + سلاسل شهرية — مُختبَرة.
- [ ] الحسابات (Chart of Accounts) بأسلوب Odoo.
- [x] الصلاحيات والمستخدمين — 8 أدوار مهنية + مصفوفة صلاحيات + استثناءات فردية — مُختبَرة.
- [x] سجل التدقيق (Audit) — activitylog على الوحدات + شاشة سجل النشاط.
- [x] إدارة الموقع — شرائح الهيرو وإعدادات الموقع من لوحة التحكم.

## 5) المراجعة والتدقيق الشامل (مطلوب صراحةً في الوصف)
- [ ] فحص قاعدة البيانات والعلاقات بين الوحدات.
- [x] مراجعة الصلاحيات (RBAC) — مراجعة 2026-09-11: أُصلح كشف مدير الملفات لكل ملفات المكتب لكل من يملك `documents.view` (أُضيفت `documents.view.all`).
- [x] مراجعة الأداء — مراجعة 2026-09-11: تقسيم حزمة الواجهة (1.6MB ← 413KB)، وفهرسة أعمدة النبضة، وفصل حدّ معدّلها.
- [x] مراجعة الأمان — مراجعة 2026-09-11: أُصلح XSS المستندات المولّدة في بوابة العميل، ونُزعت مفاتيح Supabase من الكود، وضُبطت صلاحية التوكن. **مطلوب من العميل: إبطال مفتاح Supabase القديم من لوحة التحكم.**
- [ ] اكتشاف وتوثيق الأخطاء الحالية (Bugs) قبل الإصلاح.

## 6) تطوير الواجهة (UI/UX)
- [ ] تصميم حديث احترافي (ألوان، خطوط، كروت، تباعد).
- [ ] توحيد نظام تصميم (Design System) عبر المنصة.
- [ ] تحسين تدفق الاستخدام وتجربة المستخدم.
- [ ] تحسين نسخة الجوال والمتصفح للـERP.

## 7) فترة التجربة والتسليم النهائي
- [ ] فترة تجربة يسجّل فيها العميل الملاحظات والأخطاء أثناء الاستخدام.
- [ ] معالجة الملاحظات والأخطاء الظاهرة.
- [ ] التسليم النهائي بعد الاستقرار.
- [ ] وثيقة/فيديو تشغيل مختصر للعميل.

---

# الجزء الثاني — النقاط التقنية

## 8) إعداد مشروع Laravel  ✅ *(الأساس مكتمل ومُختبَر — انظر [BACKEND_ARCHITECTURE_AR.md](BACKEND_ARCHITECTURE_AR.md))*
- [x] تثبيت Laravel 13 + المكتبات (Sanctum, spatie permission/activitylog/query-builder, dompdf, larastan, pint) + ربط MySQL — مُختبَر.
- [x] إعداد `.env` (DB/Queue/Cache/CORS/Sanctum) — الأسرار في `.env` فقط.
- [x] هيكلة الطبقات: Controllers/Api + Requests + Resources + Services + Actions + Enums + Policies + DTOs + Support.
- [x] الظرف الموحّد `ApiResponse` + معالجة أخطاء مركزية (مُختبَر 200/401/404/422).
- [x] الأمان: SecurityHeaders + ForceJsonResponse + CORS مقيّد + Rate limiting (api/auth) — مُختبَر.
- [x] جودة الكود: Pint نظيف · 426 اختبارًا ناجحًا. ⚠️ PHPStan level 6 مضبوط لكنه لا يعمل على PHP 8.3.33 بجهاز التطوير (ينهار بلا مخرجات) — يحتاج تحقّقًا في بيئة أخرى.
- [ ] Reverb (WebSockets) + Scheduler — لاحقًا عند وحدة الدردشة/الإشعارات.

## 9) قاعدة البيانات MySQL (مخطط موحّد)
- [ ] تصميم **مخطط واحد نظيف** يجمع كل الجداول ويحلّ تضارب المخططين الحاليين:
  - مستخدمين/أدوار/صلاحيات · جلسات
  - CRM: عملاء/شركات/جهات اتصال/leads (Pipeline بأسلوب Odoo)
  - مشاريع/مراحل/أعضاء/محادثات · مهام/سجلات وقت
  - مواعيد/اجتماعات (+ فيديو) · ملفات/صلاحيات ملفات
  - التسعير: خدمات/باقات/رسوم/شرائح · عروض أسعار (quotations + items)
  - العقود · الفواتير/المدفوعات/المصروفات · الرواتب · الحضور
  - المنتدى: أقسام/مواضيع/ردود · معرض الأعمال (portfolio)
  - إشعارات · إعدادات النظام · سجل التدقيق (audit)
- [ ] كتابة **Migrations** لكل الجداول + العلاقات (Foreign Keys) + الفهارس.
- [ ] كتابة **Seeders** (أدوار افتراضية، مستخدم أدمن، بيانات التسعير، بيانات تجريبية).
- [ ] بناء **Eloquent Models** بالعلاقات (hasMany/belongsTo/…).

## 10) تحويل المشروع للعمل عبر API (Backend)
- [ ] بناء **Controllers + Routes** لكل وحدة تحت `/api/v1/...` تُرجع JSON.
- [ ] **نقل** منطق الوحدات القائمة من Node إلى Laravel (auth, users, roles, projects, tasks, notifications, files, calendar, appointments, audit, chatbot).
- [ ] **بناء الوحدات الناقصة**: التسعير، عروض الأسعار، المستندات، الحضور، الرواتب، العقود، CRM، الفواتير/المالية، المنتدى.
- [ ] Validation عبر **Form Requests** + تنسيق الرد عبر **API Resources**.
- [ ] معالجة أخطاء موحّدة + Pagination + بحث/فلترة.

## 11) تحويل الواجهة للعمل عبر API (Frontend)
- [ ] بناء طبقة اتصال موحّدة في الواجهة (`apiClient` بـ`fetch` + توكن Sanctum).
- [ ] **استبدال** `shared/supabase-client.js` / `MemarDB` / الاتصال المباشر بـ Supabase باستدعاءات الـAPI.
- [ ] **إزالة الاعتماد على `localStorage` الوهمي** والبيانات التجريبية (demo) في `erp_app.js`.
- [ ] ربط كل شاشة ERP ببياناتها الحقيقية من الـAPI (dashboard, CRM, projects, tasks, HR, finance…).
- [ ] ربط تسجيل الدخول بـ Sanctum + حفظ التوكن + التوجيه حسب الدور.
- [ ] الدردشة/الإشعارات اللحظية عبر Laravel Echo (بدل Socket.io).

## 12) الأمان
- [ ] مصادقة Sanctum + تشفير كلمات المرور (bcrypt).
- [ ] **صلاحيات على مستوى كل endpoint** (Policies / Gates / Middleware) — بأسلوب RBAC الحالي.
- [ ] إزالة أي وصول مفتوح للبيانات + إخفاء كل المفاتيح في `.env` على السيرفر.
- [ ] حماية من CSRF/XSS/SQL Injection (Eloquent يحمي افتراضيًا) + Rate Limiting.
- [ ] التحقق من الملفات المرفوعة (نوع/حجم) + تخزين آمن.

## 13) تنظيف المشروع وترتيبه
- [ ] حذف السكربتات المتناثرة (`update_*.js`, `fix_*.js`, `scratch/`, `deprecated/`) والملفات الفارغة.
- [ ] إزالة المحتوى المكرّر بين الجذر و`website/`.
- [ ] فصل الواجهة الضخمة `erp_app.js` (752KB) إلى وحدات منطقية أصغر (قدر المتاح ضمن الوقت).
- [ ] إدخال نظام build/تجميع بسيط للواجهة + إدارة الإصدارات (cache-busting).

## 14) النشر على سيرفر Synology (Production)
- [ ] تجهيز PHP + MariaDB على الـNAS (Web Station / Docker).
- [ ] نشر Laravel + ضبط البيئة + تشغيل Migrations.
- [ ] Reverse Proxy (Nginx) + SSL (HTTPS) + الدومين.
- [ ] تشغيل Queue Worker + Scheduler + Reverb بشكل دائم.
- [ ] نسخ احتياطي دوري (قاعدة البيانات + الملفات) + مراقبة.
- [ ] اختبار استقرار شامل بعد النشر.

---

## بنية المشروع (بعد إعادة الهيكلة 2026-07-15)
```
memar-platform-v2/
├── PLAN.md · PORT_REGISTRY_NOTE.md · README.md · .gitignore   (الجذر)
├── backend-memar/    Laravel 13 + MySQL  ← قيد البناء
├── frontend-memar/   الواجهات (erp/website/portal/shared) ← نعيد ربطها على API
└── legacy-memar/     المشروع الأصلي (Node/Express+Supabase) — مرجعية، يُحذف لاحقاً
```
المنافذ: Laravel **8010** · Frontend **3015** · MySQL **3306** (قاعدة `memar`).

## سجل القرارات
- 2026-07-15 — اعتماد MySQL (XAMPP/MariaDB) بدل Supabase → معمارية 3 طبقات عبر API.
- 2026-07-15 — تنفيذ كل الميزات المضمونة ضمن عقد المنصة ($900).
- 2026-07-15 — اعتماد **Laravel (PHP)** للباك اند بدل Node/Express → تحويل كامل + إعادة ربط الواجهة على API.
- 2026-07-15 — إعادة هيكلة الجذر (backend/frontend/legacy) بنمط مشاريع المستخدم + تثبيت Laravel 13 مُختبَر على MySQL.
- 2026-07-15 — قرار **إعادة بناء الواجهة بـ React + Vite + TypeScript** (بدل JS الخام) مع الحفاظ على التصميم (إعادة استخدام CSS الموروث). مشروع جديد `frontend-react-memar/`. الأساس جاهز ومُختبَر بصريًا (تسجيل دخول + لوحة تحكم + قائمة جانبية + اتصال API عبر بروكسي). يبقى `frontend-memar` كمرجع تصميم/منطق.

---

## 📊 جدول التقدم (آخر تحديث: 2026-07-15)

> **الحالة العامة (2026-09-11):** المنصة مبنية وتعمل على المكدّس الجديد (Laravel + MySQL + React).
> المتبقّي الأكبر: **النشر على سيرفر العميل** وإكمال بعض الميزات المضمونة.
> **ملاحظة:** النِسَب تعكس الجاهزية على المكدّس الجديد وحده — `legacy-memar` و`frontend-memar` مرجعيّان ولا يُحتسبان.

| # | المجال | الحالة | النسبة |
|:-:|--------|--------|:------:|
| — | **الإعداد والبنية** (هيكلة + Laravel + معمارية + قاعدة البيانات) | ✅ منجز | **100%** |
| — | ├ إعادة الهيكلة (backend/frontend/legacy) | ✅ منجز | 100% |
| — | ├ تثبيت Laravel + ربط MySQL (مُختبَر) | ✅ منجز | 100% |
| — | ├ المعمارية الاحترافية (طبقات/أمان/جودة) مُختبَرة | ✅ منجز | 100% |
| — | └ Migrations + Models + Seeders | ✅ منجز — 116 migration · 60 model · 21 seeder | 100% |
| 1 | نشر وتشغيل المنصة على السيرفر | ⬜ لم يبدأ | 0% |
| 2.1 | التسعير التلقائي | 🟡 جدول الخدمات وشاشته مبنيان — الباقات والرسوم وشرائح المساحات ناقصة | 35% |
| 2.2 | عروض الأسعار (Quotations) | 🟢 حفظ وترقيم وحالات وتحويل لعقد+فواتير، مُختبَرة — ناقص: PDF وربط محرك التسعير | 75% |
| 2.3 | أتمتة المستندات | 🟢 قوالب + محرك تعبئة مهرَّب + حفظ وربط بالمشروع — ناقص: PDF خادم و ZIP | 70% |
| 2.4 | الشات بوت | 🟢 OpenAI + ردود بالقواعد كخط أول — ناقص: قاعدة معرفة قابلة للتحرير ولوحتها | 65% |
| 2.5 | الحضور/الانصراف (البصمة ⏸️ مؤجّلة) | 🟢 تسجيل + موقع جغرافي + سجل وملخّص — ناقص: ربط خصومات الرواتب | 80% |
| 2.6 | اجتماعات الفيديو | 🟡 غرفة Jitsi تُولَّد وتُربط بالموعد — الدخول ما زال عبر رابط خارجي | 45% |
| 3 | الصفحة الرئيسية + الهيرو + معرض الأعمال | 🟢 مبنية ومُدارة من اللوحة — ناقص: تحسين الجوال | 80% |
| 3 | المنتدى (Forum) | 🟢 أقسام/مواضيع/ردود + صلاحيات، مُختبَر — ناقص: الإعجاب وأفضل إجابة | 80% |
| 4 | وحدات الـERP (نقل على API صفحة صفحة) | 🟢 41 وحدة واجهة مقابل 37 وحدة مسارات — ناقص: شجرة الحسابات وتسجيل الوقت و RSVP | 85% |
| 5 | المراجعة والتدقيق الشامل | 🟢 مراجعة شاملة 2026-09-11 (أمان/صلاحيات/أداء) وأُصلحت نتائجها | 80% |
| 6 | تطوير الواجهة (UI/UX) | 🟢 نظام تصميم موحّد عبر المنصة — ناقص: تلميع نسخة الجوال | 70% |
| 7 | فترة التجربة والتسليم | ⬜ لم يبدأ | 0% |
| 8 | إعداد Laravel + المعمارية الاحترافية | 🟢 مكتمل ومُختبَر (Reverb لاحقًا · PHPStan يحتاج بيئة تعمل) | 90% |
| 9 | قاعدة البيانات MySQL (تنفيذ) | ✅ منجز — المخطط منفَّذ بالكامل بعلاقات وفهارس | 100% |
| 10 | تحويل الباك اند للعمل عبر API | 🟢 58 متحكّمًا · 37 خدمة · 37 وحدة مسارات · 426 اختبارًا ناجحًا | 90% |
| 11 | إعادة بناء الواجهة بـ React + ربطها على API | 🟢 41 وحدة على TanStack Query + تقسيم الحزم | 85% |
| 12 | الأمان | 🟢 Sanctum بصلاحية 7 أيام · RBAC على كل endpoint · ترويسات وCORS وRate limiting · تنقية XSS · فحص المرفوعات — ناقص: إبطال مفتاح Supabase القديم (على العميل) | 85% |
| 13 | التنظيف والترتيب | 🟡 الهيكلة تمّت والأسرار نُزعت — ناقص: حذف legacy-memar و frontend-memar بعد اكتمال النقل | 60% |
| 14 | النشر على Synology | ⬜ لم يبدأ | 0% |

### الإجمالي المُقدَّر (مرجّح بالجهد)

| المكوّن | الوزن | الإنجاز |
|--------|:----:|:------:|
| الإعداد والبنية + قاعدة البيانات | 15% | 100% |
| الميزات الست المضمونة | 30% | 62% |
| وحدات الـERP + ربطها على API | 30% | 87% |
| الموقع العام + المنتدى | 10% | 80% |
| الأمان + التنظيف + النشر + التجربة | 15% | 41% |
| **الإجمالي** | **100%** | **≈ 74%** |

**الخلاصة (محدَّثة 2026-09-11):** المنصة مبنية وتعمل — قاعدة البيانات والباك اند والواجهة وكل وحدات
الـERP تقريبًا، بـ426 اختبارًا خلفيًّا ناجحًا. الشغل المتبقّي ثلاثة أثلاث:

1. **النشر** (بندا 1 و14) — لم يُبدأ، وهو ما يراه العميل أولًا: Synology + دومين + SSL + نسخ احتياطي.
2. **إكمال الميزات المضمونة** — PDF على الخادم (عروض ومستندات)، ربط محرك التسعير، ربط الحضور بالرواتب،
   دمج الفيديو داخل المنصة، ودفع KNET.
3. **التنظيف والتجربة** — حذف `legacy-memar` و`frontend-memar` بعد اكتمال النقل، ثم فترة تجربة العميل.
